/*
const person1 = {
    name: "jack",
    age: "16-26"
};

const person2 = {
    name: "baby",
    age: "27-55"
};

function SuggestMusic(person){
    if(person.age === "16-26"){
        console.log("Rock music is suited for you");
    }
    else if(person.age === "27-55"){
        console.log("No music for you go to work");
    }
    else{
        console.log("umaiwa mausinde!!");
    }
}

SuggestMusic(person1);
SuggestMusic(person2);

const dog={
    sound: "woof woof",
    speak(){
        console.log("woof woovweeeee");
    }
}
dog.speak();
console.log(dog.sound);

const cities=[
    "Ranchi",
    "Daltonganj",
    "Delhi",
    "Pune",
    "Bangalore"
]

for(let i=0;i<=cities.length;i++){
   // console.log(cities[i]);
    //console.log(cities[i].toUpperCase())
    console.log(cities[i].toLowerCase())
}
*//*
const me = {
    name: {
      first: "Brian",
      last: "Holt"
    },
    location: {
      streetNumber: 500,
      street: "Fakestreet",
      city: "Seattle",
      state: "WA",
      zipCode: 55555,
      country: "USA"
    },
    getAddress() {
      return `${this.name.first} ${this.name.last}
  ${this.location.streetNumber} ${this.location.street}
  ${this.location.city}, ${this.location.state} ${this.location.zipCode}
  ${this.location.country}`;
    }
  };
  
  console.log(me.getAddress());

*/

let evt = document.getElementsByClassName("js-func");
document.evt.addEventListner("click", function(){ //error
    eve.textContent = "hi";
})